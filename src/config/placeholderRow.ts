/** Special ID for the placeholder row — used by paste logic and selection. */
import type { TaskId } from "../types/branded.types";

export const PLACEHOLDER_TASK_ID = "__new_task_placeholder__" as TaskId;
